Internet Basics
UI/UX Design(figma)
HTML 
  -- intro
  -- headings,p,a,img
  -- lists
  -- formatting tags
  -- tables
  -- forms(inputs,validation)
  -- block vs inline,semantic
  -- media,HTML5
CSS
  -- intro
  -- bg,text,fonts,colors
  -- float,display
  -- position
  -- flex
  -- grid
  -- media queries
  -- pesudo elements & attributes
  -- transitions & animations
Bootstrap
  -- netlify,vscode
JS
  -- intro
  -- var,let,const
  -- JS engine(callstack,hoisting,execution content, window object etc..,)
  -- number(math methods),string(template literal, default methods),boolean,undefined,null
 -- functions(normal function, return function,default parameters,rest,spread,callback,closure,function expression,arrow)
 -- Arrays(basics, push,pop,shift,unshift,splice,includes,concat,reverse,sort,
 forEach,map,filter,some,every,reduce)
 -- objects(dot,index signature,Object methods)
 -- DOM(Document Object Model)
     -- selection:getElementsByTagName,getElementsByClassName,getElementByID,
     querySelector,querySelectorAll
     -- manipulation: textContent,innerText,innerHTML,
                      classlist.add/remove/toggle
                      style
     -- creation : createElement,append,appendChild
-- events : event handlers
            event listners
                 -- bubbling
                 -- capturing
                 -- delegation
-- AJAX(Asynchronous js)
    -- timers(setTimeout,setInterval)
    -- API,json,endpoint
    -- promises
    -- fetch
    -- async await
-- ES6 
    --OOPS (classes,constructors,inheritance)
    -- destructuring
    -- modules
-- BOM
-- libraries

















