JS
==================
  1. introduction
  2. var,let,const
  3. Working of JS( Hoisting, callstack, execution content, console)
  4. Data types
    i. primitive
          number: math methods
          strings: string methods, template literals
          boolean
          undefined
          null
    ii. functions
            normal,with parameters, default parameters,
            return values, rest,spread,function expression
            callback, closure
    iii. Arrays:
            basics, length,push,pop,unshift,shift,splice,
            includes,indexOf,lastIndexOf,concat,sort,reverse,
            forEach,map,filter,some,every,reduce
    iv. objects: dot or indexSignature
    Object methods    

3. control statements
     -- if
     -- switch
4. loops 
    -- while
    -- do while
    -- for
    -- forEach
    -- for..of
    -- for..in
5. DOM
    -- selection
    -- manipulation
    -- creation
6. Events
    -- event handlers
       --mouse events,keyboard events,form events,window events
    -- event listners
       -- event propagation(bubbling,capturing),event delegation
7. Asynchronous Javascript
   -- timers
        -- setTimeout,clearTimeout
        -- setInterval,clearInterval
   -- Promises
   -- API(Application programming interface)
   -- async await
8. ES6
   -- let,const,var
   -- template reference variable
   -- rest,spread
   -- arrow functions
   -- destructuring
   -- OOPS,classes,object,inheritamce,constructor
   -- modules
   -- Set,Map,Symbol,RegExp
9. BOM
10. libraries like jquery
   






